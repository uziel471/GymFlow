// Deprecated: the mock cookie session was replaced by credential + JWT auth
// in `auth.action.ts`. This file is intentionally empty (the sandbox
// filesystem does not allow deleting it) and is not imported anywhere.
export {};
